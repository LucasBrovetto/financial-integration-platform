spring:
  application:
    name: transaction-service
  profiles:
    default: local
  jpa:
    open-in-view: false
  flyway:
    locations: classpath:db/migration
server:
  port: ${SERVER_PORT:8080}

management:
  endpoints:
    web:
      exposure:
        include: health,info
